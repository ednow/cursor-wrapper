---
description: 新增或修改配置项时须同步 app/config.py 与 app/config.bak.py
alwaysApply: true
---

# 配置文件成对维护

本仓库的应用配置定义在 `app/config.py`；`app/config.bak.py` 为备份/对照模板，结构应与 `config.py` 保持一致。

## 要求

- **新增、重命名、删除或调整任何配置项**（含默认值、类型、注释、分组）时，必须**同时**修改：
  - `app/config.py`
  - `app/config.bak.py`
- 两处改动应在同一次提交中完成，避免只改其一导致部署或对照不一致。
- 若某处为占位或敏感说明，两处仍应保持字段与结构对齐；仅文案差异需在注释中说明原因。

## 自检

提交前确认：与配置相关的 diff 中是否**同时**出现上述两个文件的对应变更。
